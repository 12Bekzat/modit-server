package internet.magazine.magazine.product;

import org.springframework.data.jpa.repository.JpaRepository;

public interface CatalogImportSettingsRepository extends JpaRepository<CatalogImportSettings, Long> {
}
